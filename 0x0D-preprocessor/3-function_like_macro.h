#ifndef FUNCTION_LIKE_MACRO_H
#define FUNCTION_LIKE_MACRO_H

/*
 * File: 3-function_like_macro.h
 * Desc: Header file that create function like macro
 *       ABS(x) that computes the absolute value of num.
 */


#define ABS(x) ((x) < 0 ? ((x) * -1) : (x))

#endif
